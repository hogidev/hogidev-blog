version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx ng build --configuration production
  artifacts:
    baseDirectory: dist/hogidev-blog/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
